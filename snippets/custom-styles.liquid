{% comment %}
  Here's an example of a customization
  You can paste this at the bottom of the page to see how it works

  /* Author - Date - Customization */
  /* E.g. Troop Themes - 12th November .... - Changed paragraph colors */
  @media only screen and (max-width: 767px) { /* mobile */
    body {
      color: blue;
    }
  }

  @media only screen and (min-width: 768px) { /* desktop */
    body {
      color: green;
    }
  }

  /* Add your theme customizations below */
{% endcomment %}

@media only screen and (min-width: 767px) {
  .featured-collection--root .featured-collection--grid {
    display:flex;
    flex-wrap: wrap;
    justify-content: center;
  }
  .featured-collection--root .featured-collection--grid .product--root {
    flex:0 0 calc(33.333% - 32px);
  }
}
.featured-video--sub-heading, .featured-video--sub-heading-mobile {
    font-family:var(--font--paragraph--family);
    font-weight:normal;
}
.newsletter--root .newsletter--wrapper {
    gap:32px;
}
.newsletter--heading {
	font-size:var(--font--featured-heading--size);
}
.newsletter--subheading {
  	font-size:var(--font--block-heading--size);
  	letter-spacing:.025em;
    line-height: 1.33333em;
    width:100%;
}
.flexbox-gap [data-background-color=dark] .featured-content--text-container {
  gap:32px;
}
.featured-collection--show-more {
  padding-top:32px;
  z-index:1;
}
[data-background-color=dark] .featured-content--link--container, [data-light-text=true] .featured-collection--show-more a {
  background: var(--color--button);
  border-color:var(--bg-color--button);
  border-radius:2px;
  color: var(--bg-color--button);
  letter-spacing: 1px;
  font-size: 14px;
  height: 45px;
  line-height: 45px;
  padding: 0 16px;
  vertical-align: bottom;
  width: auto;
}
[data-background-color=dark] .featured-content--link--container:after, .featured-collection--show-more a:after {
  display:none;
  visibility:hidden;
}
[data-light-text=true] .featured-video--button>* {
  background: var(--color--button);
  border-color:var(--color--button);
  color: var(--bg-color--button);
}
@media only screen and (max-width: 767px) {
  .featured-video--root .featured-video--header-mobile {
    padding:64px 32px;
  }
  .newsletter--heading{font-size:28px;}
  .newsletter--subheading{font-size:18px;}
}
.footer--root .footer--blocks, .footer--root .footer--sub  {
  flex-direction: column;
  align-items: center;
  text-align: center;
}
@media only screen and (min-width: 767px) {
  .footer--menu li {
    display:inline-block;
    letter-spacing:.05em;
    padding:0 8px;
  }
}
.featured-collection--title {    
  display: none!important;
}
.product--root .product--label-container[data-item] {
    top: calc(50% - 13.5px);
    left: calc(50% - 61px);
}
.product--root[data-label-shape=round] .product--label {
  padding: 11px 10px;
  border-radius: 100px;
  background: white;
  color: black;
  font-size: small;
}
.product--title {  
  font-size: large;
}
.product--price.money {
font-size: large;
}
.product--details {   
  flex-direction: column;
}
.product-details-container {
  align-items: center;
}
